{
  "name": "translate-to-english",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "cd angular-app/translate-app && npm install && ng build",
    "postinstall": "cd angular-app/translate-app && npm install && ng build",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "openai": "^4.20.1",
    "form-data": "^4.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "@angular/cli": "^17.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}